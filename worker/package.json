{
  "name": "visiood-mediator-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "set-github-token": "wrangler secret put GITHUB_TOKEN",
    "set-composio-key": "wrangler secret put COMPOSIO_API_KEY",
    "set-api-key": "wrangler secret put API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
